# Operations Guide

### Deployment
* Automatically deployed on **Render** platforms connected directly to the main branch of GitHub.

### Environment
* Node.js Runtime environment.
* Port is dynamically allocated using `process.env.PORT`.

### Monitoring
* Standard output (console.log) formatted as JSON strings for clean production log indexing.

